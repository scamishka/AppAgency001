$( document ).ready(function() {
    let attrName = $('.mortgage-section .cetacs-block_text-block_block-img').attr('data-img-position');
    switch (attrName) {
        case img-left:
            break;
        case img-right:
            $('.mortgage-section .cetacs-block_text-block_block-img').addClass()
    }
    $('.mortgage-section .cetacs-block_text-block_block-img').addClass(attrName);
});
