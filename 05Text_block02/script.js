$( document ).ready(function() {
    let attrName = $('.cetacs-block_text-block_block-img').attr('data-img-position');
    switch (attrName) {
        case img-left:
            break;
        case img-right:
            $('.cetacs-block_text-block_block-img').addClass()
    }
    $('.cetacs-block_text-block_block-img').addClass(attrName);
});
